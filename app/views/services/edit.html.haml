- provide(:title, 'Edit Service')

.row
  .small-12.columns
    %h1.main_page_header Editing Service
    .panel
      = render 'form'
      = link_to "#{current_account.name} Account", current_account, class: 'small button radius'
