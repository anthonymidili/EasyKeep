.row
  .small-12.medium-4.large-4.push-8.columns
    .visible-for-small-only.hide-on-print
      = image_tag current_company.logo_url(:thumb) if current_company.logo.present?
    .visible-for-medium-up
      .right= image_tag current_company.logo_url(:thumb) if current_company.logo.present?

  = render 'companies/info'
